{
  "name": "somnia-bridge-dapp",
  "version": "1.0.0",
  "description": "🪙 Simple Bridge DApp on Somnia Testnet (Vault + WrappedToken)",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network somnia"
  },
  "keywords": [
    "somnia",
    "bridge",
    "defi",
    "hardhat",
    "web3"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "ethers": "^6.7.0"
  }
}
